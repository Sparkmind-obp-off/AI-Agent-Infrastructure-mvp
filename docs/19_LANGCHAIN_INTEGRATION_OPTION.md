# LangChain Integration

LangChain is not a core MVP dependency.

Use native Cloudflare Agents SDK, MCP and internal provider interfaces first.

A LangChain adapter may be added later when a concrete workflow needs a LangChain-specific capability that materially reduces implementation cost.

Do not introduce LangChain only for abstraction's sake.
